@model STEM.Web.Areas.Admin.Models.ClassDetailsViewModel

@{
    ViewData["Title"] = "Chi tiáº¿t lá»›p há»c";
}

<section class="admin-page">
    <div class="admin-page-header">
        <div>
            <h1 class="admin-page-title">Chi tiáº¿t lá»›p há»c</h1>
            <p class="admin-page-subtitle">Theo dÃµi lá»›p, thÃªm há»c viÃªn vÃ  má»Ÿ cÃ¡c buá»•i há»c liÃªn quan.</p>
        </div>

        <div class="admin-actions">
            <a asp-area="Admin" asp-controller="Sessions" asp-action="Create" asp-route-classId="@Model.Id" class="admin-btn-secondary">ThÃªm buá»•i há»c</a>
            <a asp-area="Admin" asp-controller="Classes" asp-action="Edit" asp-route-id="@Model.Id" class="admin-btn-primary">Cáº­p nháº­t</a>
            <a asp-area="Admin" asp-controller="Classes" asp-action="Index" class="admin-btn-secondary">Quay láº¡i</a>
        </div>
    </div>

    @if (TempData["SuccessMessage"] is string successMessage)
    {
        <div class="admin-alert admin-alert--success">@successMessage</div>
    }

    @if (TempData["ErrorMessage"] is string errorMessage)
    {
        <div class="admin-alert admin-alert--error">@errorMessage</div>
    }

    <div class="admin-grid-two">
        <div class="space-y-6">
            <div class="admin-card">
                <div class="admin-card-section">
                    <h2 class="admin-section-title">ThÃ´ng tin lá»›p há»c</h2>

                    <dl class="admin-data-grid two-col mt-6">
                        <div class="admin-data-item">
                            <dt>MÃ£ lá»›p</dt>
                            <dd>@Model.ClassCode</dd>
                        </div>
                        <div class="admin-data-item">
                            <dt>KhÃ³a há»c</dt>
                            <dd>@Model.CourseName (@Model.CourseCode)</dd>
                        </div>
                        <div class="admin-data-item">
                            <dt>GiÃ¡o viÃªn phá»¥ trÃ¡ch</dt>
                            <dd>@Model.TeacherName</dd>
                        </div>
                        <div class="admin-data-item">
                            <dt>LiÃªn há»‡ giÃ¡o viÃªn</dt>
                            <dd>@(string.IsNullOrWhiteSpace(Model.TeacherEmail) ? Model.TeacherPhone ?? "ChÆ°a cáº­p nháº­t" : Model.TeacherEmail)</dd>
                        </div>
                        <div class="admin-data-item">
                            <dt>NgÃ y báº¯t Ä‘áº§u</dt>
                            <dd>@Model.StartDate.ToString("dd/MM/yyyy")</dd>
                        </div>
                        <div class="admin-data-item">
                            <dt>NgÃ y káº¿t thÃºc</dt>
                            <dd>@(Model.EndDate?.ToString("dd/MM/yyyy") ?? "Äang má»Ÿ")</dd>
                        </div>
                    </dl>
                </div>
            </div>

            <div class="admin-card">
                <div class="admin-card-section">
                    <div class="flex flex-col gap-4 xl:flex-row xl:items-start xl:justify-between">
                        <div>
                            <h2 class="admin-section-title">ThÃªm há»c viÃªn vÃ o lá»›p</h2>
                            <p class="admin-section-subtitle">TÃ¬m theo tÃªn hoáº·c username rá»“i chá»n nhiá»u há»c viÃªn cÃ¹ng lÃºc.</p>
                        </div>

                        <form method="get" class="flex w-full flex-col gap-3 xl:max-w-[420px]">
                            <input type="hidden" name="id" value="@Model.Id" />
                            <div class="relative">
                                <span class="material-symbols-outlined pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-[20px] text-[#73796b]">search</span>
                                <input name="studentSearch"
                                       value="@Model.StudentSearchTerm"
                                       class="admin-input pl-12"
                                       placeholder="TÃ¬m theo tÃªn hoáº·c username" />
                            </div>
                            <button type="submit" class="admin-btn-secondary w-full justify-center">Lá»c danh sÃ¡ch</button>
                        </form>
                    </div>

                    @if (Model.AvailableStudents.Count == 0)
                    {
                        <div class="admin-empty">KhÃ´ng cÃ²n há»c viÃªn phÃ¹ há»£p Ä‘á»ƒ thÃªm vÃ o lá»›p nÃ y.</div>
                    }
                    else
                    {
                        <form asp-area="Admin" asp-controller="Classes" asp-action="EnrollStudents" method="post" class="mt-6 space-y-4">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@Model.Id" />

                            <div class="flex flex-wrap items-center justify-between gap-3">
                                <p class="text-sm text-[#73796b]">@Model.AvailableStudentCount há»c viÃªn Ä‘ang hiá»ƒn thá»‹</p>
                                <label class="inline-flex items-center gap-2 text-sm font-medium text-[#42493d]">
                                    <input type="checkbox" data-select-all-students class="h-4 w-4 rounded border-[#c2c9b9] text-[#1a3a6e] focus:ring-[#1a3a6e]" />
                                    <span>Chá»n táº¥t cáº£</span>
                                </label>
                            </div>

                            <div class="overflow-hidden rounded-[8px] border border-[#e3e3de]">
                                <div class="max-h-[420px] overflow-y-auto">
                                    <table class="admin-table min-w-full">
                                        <thead>
                                            <tr>
                                                <th class="w-[64px]">Chá»n</th>
                                                <th>Há»c viÃªn</th>
                                                <th>TÃªn Ä‘Äƒng nháº­p</th>
                                                <th>TrÆ°á»ng hiá»‡n táº¡i</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var student in Model.AvailableStudents)
                                            {
                                                <tr>
                                                    <td>
                                                        <input type="checkbox"
                                                               name="studentIds"
                                                               value="@student.StudentId"
                                                               data-student-checkbox
                                                               class="h-4 w-4 rounded border-[#c2c9b9] text-[#1a3a6e] focus:ring-[#1a3a6e]" />
                                                    </td>
                                                    <td><strong>@student.FullName</strong></td>
                                                    <td>@student.Username</td>
                                                    <td>@(string.IsNullOrWhiteSpace(student.SchoolName) ? "ChÆ°a cáº­p nháº­t" : student.SchoolName)</td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            </div>

                            <div class="flex flex-wrap items-center justify-between gap-3">
                                <p class="text-sm text-[#73796b]"><span data-selected-count>0</span> há»c viÃªn Ä‘Ã£ chá»n</p>
                                <button type="submit" class="admin-btn-primary min-w-[220px] justify-center">ThÃªm há»c viÃªn Ä‘Ã£ chá»n</button>
                            </div>
                        </form>
                    }
                </div>
            </div>

            <div class="admin-card">
                <div class="admin-card-section">
                    <h2 class="admin-section-title">Há»c viÃªn trong lá»›p</h2>
                    <p class="admin-section-subtitle">Danh sÃ¡ch há»c viÃªn Ä‘Ã£ ghi danh.</p>

                    @if (Model.Students.Count == 0)
                    {
                        <div class="admin-empty">Lá»›p nÃ y chÆ°a cÃ³ há»c viÃªn nÃ o.</div>
                    }
                    else
                    {
                        <div class="mt-6 overflow-hidden rounded-[8px] border border-[#e3e3de]">
                            <table class="admin-table min-w-full">
                                <thead>
                                    <tr>
                                        <th>Há»c viÃªn</th>
                                        <th>TÃªn Ä‘Äƒng nháº­p</th>
                                        <th>NgÃ y ghi danh</th>
                                        <th class="text-right">Thao tÃ¡c</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var student in Model.Students)
                                    {
                                        <tr>
                                            <td><strong>@student.FullName</strong></td>
                                            <td>@student.Username</td>
                                            <td>@student.EnrollDateText</td>
                                            <td>
                                                <div class="flex justify-end">
                                                    <form asp-area="Admin" asp-controller="Classes" asp-action="RemoveStudent" method="post" class="inline-flex">
                                                        @Html.AntiForgeryToken()
                                                        <input type="hidden" name="id" value="@Model.Id" />
                                                        <input type="hidden" name="studentId" value="@student.StudentId" />
                                                        <button type="submit"
                                                                class="admin-btn-secondary !h-10"
                                                                data-confirm-submit
                                                                data-confirm-title="Gá»¡ há»c viÃªn"
                                                                data-confirm-message="Báº¡n cÃ³ muá»‘n gá»¡ há»c viÃªn nÃ y khá»i lá»›p khÃ´ng?">
                                                            Gá»¡ khá»i lá»›p
                                                        </button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                </div>
            </div>

            <div class="admin-card">
                <div class="admin-card-section">
                    <div class="flex items-center justify-between gap-4">
                        <div>
                            <h2 class="admin-section-title">Buá»•i há»c Ä‘Ã£ táº¡o</h2>
                            <p class="admin-section-subtitle">Táº¡o buá»•i má»›i hoáº·c má»Ÿ nhanh sang Ä‘iá»ƒm danh.</p>
                        </div>
                        <div class="flex gap-2">
                            <a asp-area="Admin" asp-controller="Sessions" asp-action="Generate" asp-route-classId="@Model.Id" class="admin-btn-primary !h-10">Sinh lá»‹ch tá»± Ä‘á»™ng</a>
                            <a asp-area="Admin" asp-controller="Sessions" asp-action="Create" asp-route-classId="@Model.Id" class="admin-btn-secondary !h-10">ThÃªm buá»•i há»c</a>
                        </div>
                    </div>

                    @if (Model.Sessions.Count == 0)
                    {
                        <div class="admin-empty">Lá»›p nÃ y chÆ°a cÃ³ buá»•i há»c nÃ o.</div>
                    }
                    else
                    {
                        <div class="mt-6 overflow-hidden rounded-[8px] border border-[#e3e3de]">
                            <table class="admin-table min-w-full">
                                <thead>
                                    <tr>
                                        <th>Buá»•i</th>
                                        <th>NgÃ y há»c</th>
                                        <th>Khung giá»</th>
                                        <th>Chá»§ Ä‘á»</th>
                                        <th class="text-right">Thao tÃ¡c</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var session in Model.Sessions)
                                    {
                                        <tr>
                                            <td><strong>Buá»•i @session.SessionNo</strong></td>
                                            <td>@session.Date.ToString("dd/MM/yyyy")</td>
                                            <td>@session.StartTime.ToString("HH:mm") - @session.EndTime.ToString("HH:mm")</td>
                                            <td>@(string.IsNullOrWhiteSpace(session.Topic) ? "ChÆ°a cáº­p nháº­t" : session.Topic)</td>
                                            <td>
                                                <div class="flex justify-end gap-3">
                                                    <a asp-area="Admin" asp-controller="Sessions" asp-action="Details" asp-route-id="@session.Id" class="admin-btn-secondary !h-10">Chi tiáº¿t</a>
                                                    <a asp-area="Admin" asp-controller="Attendances" asp-action="Board" asp-route-sessionId="@session.Id" class="admin-btn-secondary !h-10">Äiá»ƒm danh</a>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                </div>
            </div>
        </div>

        <aside class="space-y-6">
            <div class="admin-card">
                <div class="admin-card-section">
                    <h2 class="admin-section-title">Tráº¡ng thÃ¡i</h2>
                    <div class="mt-5">
                        <span class="inline-flex items-center gap-2 rounded-full px-3 py-2 text-sm font-semibold @Model.StatusBadgeClass">
                            <span class="h-2 w-2 rounded-full @(Model.StatusLabel == "Äang má»Ÿ" ? "bg-[#456c3f]" : Model.StatusLabel == "Táº¡m dá»«ng" ? "bg-[#9b682f]" : Model.StatusLabel == "ÄÃ£ há»§y" ? "bg-[#b33a3a]" : "bg-[#42493d]")"></span>
                            @Model.StatusLabel
                        </span>
                    </div>
                </div>
            </div>

            <div class="admin-card">
                <div class="admin-card-section">
                    <h2 class="admin-section-title">Tổng quan lớp</h2>
                    <div class="mt-5 grid gap-3">
                        <div class="rounded-xl border border-[#e3e9f2] bg-[#f8fbff] px-4 py-3">
                            <div class="text-xs font-semibold uppercase tracking-[0.08em] text-[#6b7c99]">Độ tuổi phù hợp</div>
                            <div class="mt-1 text-2xl font-semibold text-[#0f1e38]">@Model.AgeRangeText</div>
                        </div>
                        <div class="rounded-xl border border-[#e3e9f2] bg-white px-4 py-3">
                            <div class="text-xs font-semibold uppercase tracking-[0.08em] text-[#6b7c99]">Học phí khóa</div>
                            <div class="mt-1 text-2xl font-semibold text-[#0f1e38]">@Model.PriceText</div>
                        </div>
                        <div class="rounded-xl border border-[#e3e9f2] bg-white px-4 py-3">
                            <div class="text-xs font-semibold uppercase tracking-[0.08em] text-[#6b7c99]">Tiến độ lịch học</div>
                            <div class="mt-1 text-2xl font-semibold text-[#0f1e38]">@Model.SessionCount / @Model.TotalSessionsText</div>
                        </div>
                        <div class="rounded-xl border border-[#e3e9f2] bg-white px-4 py-3">
                            <div class="text-xs font-semibold uppercase tracking-[0.08em] text-[#6b7c99]">Sĩ số hiện tại</div>
                            <div class="mt-1 text-2xl font-semibold text-[#0f1e38]">@Model.EnrollmentCount học viên</div>
                        </div>
                        <div class="rounded-xl border border-[#e3e9f2] bg-white px-4 py-3">
                            <div class="text-xs font-semibold uppercase tracking-[0.08em] text-[#6b7c99]">Kết quả lọc hiện tại</div>
                            <div class="mt-1 text-2xl font-semibold text-[#0f1e38]">@Model.AvailableStudentCount học viên</div>
                        </div>
                    </div>
                </div>
            </div>
        </aside>
    </div>
</section>

@section Scripts {
    <script>
        (() => {
            const selectAll = document.querySelector('[data-select-all-students]');
            const checkboxes = Array.from(document.querySelectorAll('[data-student-checkbox]'));
            const countTarget = document.querySelector('[data-selected-count]');

            if (!selectAll || checkboxes.length === 0 || !countTarget) {
                return;
            }

            const updateCount = () => {
                const selectedCount = checkboxes.filter(x => x.checked).length;
                countTarget.textContent = selectedCount.toString();
                selectAll.checked = selectedCount > 0 && selectedCount === checkboxes.length;
                selectAll.indeterminate = selectedCount > 0 && selectedCount < checkboxes.length;
            };

            selectAll.addEventListener('change', () => {
                checkboxes.forEach(checkbox => checkbox.checked = selectAll.checked);
                updateCount();
            });

            checkboxes.forEach(checkbox => checkbox.addEventListener('change', updateCount));
            updateCount();
        })();
    </script>
}
